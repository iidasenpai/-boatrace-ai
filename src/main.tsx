import React from 'react';
import ReactDOM from 'react-dom/client';
import BoatRaceTool from './BoatRaceTool';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BoatRaceTool />
  </React.StrictMode>,
);
